# MC 公链 · 主网节点环境变量模板
# ============================================================================
# 用法：
#   sudo install -m 600 -o mcchain -g mcchain deploy/mainnet.env.example /etc/mcchain/mainnet.env
#   # 填好真实值后，deploy/mcchaind.service 通过 EnvironmentFile 读取
#
# ⚠️ 本文件是**模板**，绝不填入真实密钥后提交仓库。生产路径：/etc/mcchain/mainnet.env（chmod 600）。
# ⚠️ 下面四道闸门缺任何一道，链都起不来（fail-closed），而 validate-genesis 不会报错。

# ── 闸门 1：声明「本次是主网创世」 ──────────────────────────────────────────
# 与 chain-id 命名解耦：chain-id 叫什么都行，这个开关才是唯一可静态审计的判据。
# 作用：团队多签/基金会拨付地址若仍是源码可推导的占位公钥，InitGenesis 直接失败。
# 参考：x/tokenomics/keeper/genesis.go
MC_REQUIRE_REAL_GENESIS_KEYS=1

# ── 闸门 2：三个基金会/早期开发拨付地址的真实公钥（18% 总量） ──────────────
# 格式：bech32 `mcpub1...`（与 x/tokenomics/types/team_pubkeys_gen.go 同一种编码）。
# 生成方式：mcchaind keys add <name> --keyring-backend off 后取 --pubkey，
#           或用 scripts/gen_team_keys 同款工具（amino bech32 + "mcpub" HRP）。
# 建议用多签/冷钱包公钥，私钥绝不上服务器。
# 缺失后果：tokenomics.InitGenesis 返回错误 → 链起不来。
MC_FOUNDATION_EARLY_DEV_PUBKEY=
MC_FOUNDATION_OPS_PUBKEY=
MC_FOUNDATION_VESTING_PUBKEY=

# ── 闸门 3：TEE 预言机公钥（抗女巫证据链的锚点） ────────────────────────────
# 取 mcchaind oracle（或独立 oracle 服务）的 /pubkey 返回的 pubkey_base64。
# 必须与运行中的预言机签名服务同源；换密钥后要同步更新并重启节点。
# 缺失后果：app.New panic —— 生产链禁止「仅凭 SHA256(deviceID) 即可伪造」的软验证。
MC_ORACLE_PUBKEY=

# ── 闸门 4（仅本地单节点开发）：允许软验证预言机 ────────────────────────────
# 生产**必须保持注释**。设了等于放弃抗女巫证据，只应在本地起链时临时使用。
# MC_ORACLE_ALLOW_SOFT=1

# ── 其他常用 ──────────────────────────────────────────────────────────────
# 注意：以下两项**不是**环境变量，写进 Environment= 不会生效
# （误列为 env 属于「照文档配了却毫无作用」的坑）：
#   · 最低 gas 价：mcchaind start --minimum-gas-prices 0.001umc
#     （或写在 app.toml 的 minimum-gas-prices）
#   · 签名后端（tmkms）：见 docs/VALIDATOR_RUNBOOK.md —— 没有 env 开关，
#     由 tmkms 侧配置 + 节点侧移除明文私钥完成。
