vue/node_modules
vue/dist
release/
.idea/
.vscode/
.DS_Store

# === MC chain：仅源码入库，排除私钥 / 二进制 / 链状态 / 诊断垃圾 ===
testnet/
build/
mcchaind.exe
*.log
node_*.log
_*.log
diag*.ps1
unsigned.json
signed*.json
# CosmWasm wasmvm 预编译库：WSL/Linux 构建时由 wasmvm 模块自带（internal/api/），
# 项目根出现的是误放的临时文件，绝不入库。
libwasmvm*.so
libwasmvm*.dylib

# 主网启动验证临时产物（含演示 keyring 副本，绝不入库）
mainnetboot/
mainnet_*.json
mainnet_node.log
mainnet_boot.log

# 验证人私钥材料（本地 genesis 测试产物，绝不入库）
validator_key.json
priv_validator_key.json
node_key.json

# === 团队多签密钥材料（含助记词，绝密，仅本机，绝不入库）===
team_multisig_vault.txt
team_keys_gen.json
team_pubkeys_b64.txt
team_pubkeys.txt
teamkeyring/
teamkeyring_gen/

# === 构建产物 / 测试输出 / 本地运行态（绝不入库）===
*.exe
# buf 临时生成目录（真实生成代码在 x/*/types/*.pb.go，绝不入库）
_gen/
event-subscriber.exe
keeper.test.exe
testout*.txt
testout_full*.txt
test_home/
verify_home/
verify_tmp/
tmp_recover/
*_home/
*.bak

# === MC 配套项目：构建产物 / 依赖 / 本地配置 ===
mc-miner/app/build/
mc-miner/.gradle/
mc-miner/build/
mc-miner/local.properties
cosmjs-bundle/node_modules/
cosmjs-bundle/dist/
node_modules/
.gradle/
local.properties

# === 本地 PowerShell 脚本（本机工具，不入库）===
*.ps1

# === 本地工作副本 / 误放目录（锚定仓库根，避免误伤 third_party/proto/cosmos）===
/cosmos/
/mc_wp/
# Windows 盘符路径被误解析成相对路径时在仓库根落下的构建缓存目录
/e/

# Python bytecode
__pycache__/
*.pyc

# devnet 本地链数据（scripts/devnet.sh 生成，绝不入库）
.devnet*/

# 主网创世工作目录（含 keyring 副本 / gentx / 节点密钥，绝不入库）
.mainnet-genesis/
.teamverify/
.fndkeys/
